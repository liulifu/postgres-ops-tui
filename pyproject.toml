[project]
name = "postgres-ops-tui"
version = "0.1.0"
description = "A Hermes-style PostgreSQL operations TUI with a React/Ink frontend and Python gateway."
requires-python = ">=3.11"
license = "MIT"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
